// Test that we can import libDispatch in the repl.
// RUN: %lldb --repl < %s | FileCheck %s
// CHECK: Welcome to Swift

import Dispatch

let concurrentQueue = DispatchQueue(label: "queuename", attributes: .concurrent)
// CHECK: {{concurrentQueue}}: DispatchQueue = {
// CHECK-NEXT:  Dispatch.DispatchObject = {
